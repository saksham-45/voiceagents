# LMMS Text Agent

Local CLI for sending normalized LMMS commands to the `AgentControl` plugin.

## Usage

```bash
python3 cli.py "add 808"
python3 cli.py "open mixer"
python3 cli.py --interactive
```
